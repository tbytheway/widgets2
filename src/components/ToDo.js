import React, {useState} from 'react'

export default function Todo() {
    const [item, setItem] = useState()

    handleInputChange(e) {
        const name = set

    }

    return(
        <div>
            <h1>Todo App</h1>
            <input type='text' onChange={e.handleInputChange}></input>
            <button onClick={}>Submit</button>
        </div>
    )
}
